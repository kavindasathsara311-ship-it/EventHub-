{
  "name": "eventhub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix mobile run tunnel",
    "tunnel": "npm --prefix mobile run tunnel",
    "mobile": "npm --prefix mobile start",
    "backend": "npm --prefix backend start",
    "web": "npm --prefix mobile run web"
  },
  "dependencies": {
    "firebase": "^12.19.0"
  }
}
